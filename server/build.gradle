apply plugin: 'application'

mainClassName = "com.reine.server.ServerStarter"

dependencies {
    implementation project(":common")
}

tasks.withType(JavaExec) {
    setWorkingDir(new File(workingDir.getParentFile(), "run"))
}